{
  "name": "amo-solution-btp-netlify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci --legacy-peer-deps && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}